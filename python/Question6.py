# modify this function, and create other functions below as you wish
import numpy as np


portfolio=["HOQDDJRQTB", "NZXUD", "IHUFVVHGWQEM", "HCTGMLPNOVB"]
def question06(portfolio):
    # modify and then return the variable below
#    port = np.asarray(portfolio)
#    letter = [(i[0],i[-1]) for i in port]
#    
#    unique_elements, unique_inverse, unique_counts = np.unique(letter, axis=0, return_counts=True, return_inverse=True)
    
    
    return 5
